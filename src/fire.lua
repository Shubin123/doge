local fire = {}

function fire.load()
    -- Stubbed load function
end

function fire.update(dt)
    -- Stubbed update function
end

function fire.fires(n)
    -- Stubbed fires function
end

function fire.populate(world)
    -- Stubbed populate function
end

function fire.collision(fixture_a, fixture_b, contact)
    -- Stubbed collision function
end

function fire.unload()
    -- Stubbed unload function
end

-- Keep the fire table structure but with stubbed functions
return fire
